import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card";import {createClient} from "@/services/supabase/server";import {getCurrentMembership} from "@/database/memberships";export default async function AnalyticsPage(){const m=await getCurrentMembership();if(!m)return null;const s=await createClient();const [appointments,patients,messages,ai]=await Promise.all([s.from("appointments").select("id",{count:"exact",head:true}).eq("organization_id",m.organization_id),s.from("patients").select("id",{count:"exact",head:true}).eq("organization_id",m.organization_id),s.from("messages").select("id",{count:"exact",head:true}).eq("organization_id",m.organization_id),s.from("ai_interaction_logs").select("id",{count:"exact",head:true}).eq("organization_id",m.organization_id)]);return <div className="flex flex-col gap-6"><div><h1 className="font-display text-2xl font-semibold text-ink">Analytics</h1><p className="mt-1 text-sm text-ink-muted">Current operational totals from your database.</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Appointments",appointments.count],["Patients",patients.count],["Messages",messages.count],["AI interactions",ai.count]].map(([label,value])=><Card key={String(label)}><CardHeader><CardTitle>{label}</CardTitle></CardHeader><CardContent><p className="text-3xl font-semibold text-ink">{value??0}</p></CardContent></Card>)}</div></div>}
