import { Card,CardContent,CardHeader,CardTitle,CardDescription } from "@/components/ui/card";import { ActionForm } from "@/components/ui/action-form";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Button } from "@/components/ui/button";import { createClient } from "@/services/supabase/server";import { getCurrentMembership } from "@/database/memberships";import { saveWhatsAppConnection } from "@/features/whatsapp/actions";
export default async function WhatsAppSettingsPage(){const m=await getCurrentMembership();if(!m)return null;const s=await createClient();const {data}=await s.from("whatsapp_connections").select("phone_number_id,display_phone_number,business_account_id,is_active").eq("organization_id",m.organization_id).maybeSingle();return <div className="flex flex-col gap-6"><div><h1 className="font-display text-2xl font-semibold text-ink">WhatsApp</h1><p className="mt-1 text-sm text-ink-muted">Connect a WhatsApp Business phone without exposing its access token to the browser.</p></div><Card><CardHeader><CardTitle>Connection</CardTitle><CardDescription>{data?`Connected to ${data.display_phone_number??data.phone_number_id}`:"No connection configured."}</CardDescription></CardHeader><CardContent><ActionForm action={saveWhatsAppConnection} className="grid gap-4"><div><Label>Phone number ID</Label><Input name="phoneNumberId" defaultValue={data?.phone_number_id??""} required /></div><div><Label>Display phone number</Label><Input name="displayPhoneNumber" defaultValue={data?.display_phone_number??""}/></div><div><Label>Business account ID</Label><Input name="businessAccountId" defaultValue={data?.business_account_id??""}/></div><div><Label>Access token</Label><Input name="accessToken" type="password" autoComplete="new-password" required /></div><Button type="submit">Save connection</Button></ActionForm></CardContent></Card></div>}
