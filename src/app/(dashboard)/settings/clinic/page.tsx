import { requireMembership } from "@/lib/auth/tenant"; import {createClient} from "@/services/supabase/server"; import {getClinic} from "@/database/clinics"; import {ClinicSettingsForm,HoursForm} from "@/features/clinic/form"; import {saveClinicSettings,saveHours} from "@/features/clinic/actions"; import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card";
export default async function ClinicPage(){const m=await requireMembership();const s=await createClient();const clinic=await getClinic(s,m.organization_id);const hours=(clinic?.working_hours??{}) as Record<string,{open?:string;close?:string;closed?:boolean}>;return <div className="flex flex-col gap-6"><div><h1 className="font-display text-2xl font-semibold text-ink">Clinic</h1><p className="mt-1 text-sm text-ink-muted">Manage clinic settings and weekly working hours.</p></div><Card><CardHeader><CardTitle>Clinic settings</CardTitle></CardHeader><CardContent>{["owner","admin"].includes(m.role)?<ClinicSettingsForm action={saveClinicSettings} duration={clinic?.default_appointment_duration_minutes??30}/>:<p className="text-sm text-ink-muted">Clinic settings are read-only for your role.</p>}</CardContent></Card><Card><CardHeader><CardTitle>Working hours</CardTitle></CardHeader><CardContent>{["owner","admin"].includes(m.role)?<HoursForm action={saveHours} initial={hours}/>:<p className="text-sm text-ink-muted">Working hours are read-only for your role.</p>}</CardContent></Card></div>}
