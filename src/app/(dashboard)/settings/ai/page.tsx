import {Card,CardContent,CardHeader,CardTitle,CardDescription} from "@/components/ui/card";import {ActionForm} from "@/components/ui/action-form";import {Button} from "@/components/ui/button";import {createClient} from "@/services/supabase/server";import {getCurrentMembership} from "@/database/memberships";import {setReceptionistStatus} from "@/features/ai/actions";
export default async function AISettingsPage(){const m=await getCurrentMembership();if(!m)return null;const s=await createClient();const {data}=await s.from("ai_employee_instances").select("status").eq("organization_id",m.organization_id).eq("employee_type","receptionist").maybeSingle();const status=data?.status??"inactive";const can=["owner","admin","manager"].includes(m.role);return <div className="flex flex-col gap-6"><div><h1 className="font-display text-2xl font-semibold text-ink">AI Receptionist</h1><p className="mt-1 text-sm text-ink-muted">Control whether the receptionist may respond through the supported channels.</p></div><Card><CardHeader><CardTitle>{status === "active" ? "Active" : "Inactive"}</CardTitle><CardDescription>Default development provider is mock. Real providers are optional environment configuration.</CardDescription></CardHeader><CardContent>{can?<ActionForm action={setReceptionistStatus}><input type="hidden" name="status" value={status === "active" ? "inactive" : "active"}/><Button type="submit">{status === "active" ? "Deactivate" : "Activate"}</Button></ActionForm>:<p className="text-sm text-ink-muted">Only owners, admins, and managers can change this setting.</p>}</CardContent></Card></div>}
