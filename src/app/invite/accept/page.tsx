import { redirect } from "next/navigation";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { createAdminClient } from "@/services/supabase/admin";
import { createClient } from "@/services/supabase/server";
import { acceptInvite } from "@/features/team/actions";
import { InviteAcceptForm } from "@/features/team/invite-accept-form";
export default async function InviteAcceptPage({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/invite/accept");if(!user.email)return <p>Verified email required.</p>;const admin=createAdminClient();const {data:invites}=await admin.from("pending_invites").select("id,organization_id,email,role,created_at,expires_at,organizations(name)").eq("email",user.email.toLowerCase()).order("created_at",{ascending:true});return <div className="min-h-screen bg-paper px-4 py-12"><div className="mx-auto max-w-lg"><Card><CardHeader><CardTitle>Team invitations</CardTitle></CardHeader><CardContent className="flex flex-col gap-4">{!invites?.length?<p className="text-sm text-ink-muted">There are no pending invitations for {user.email}.</p>:invites.map((invite)=><div key={invite.id} className="rounded-md border border-mist p-4"><p className="font-medium text-ink">Invitation to join {(invite.organizations as {name?:string}|null)?.name ?? "your team"}</p><p className="mt-1 text-sm text-ink-muted">Role: {invite.role}</p><InviteAcceptForm action={acceptInvite} token={token ?? ""}/></div>)}</CardContent></Card></div></div>}
