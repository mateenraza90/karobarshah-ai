"use client";
import {useActionState} from "react"; import {useFormStatus} from "react-dom"; import {Button} from "@/components/ui/button"; import type {ActionState} from "@/types";
export function MemberRoleForm({action,id,role,disabled}:{action:(p:ActionState,f:FormData)=>Promise<ActionState>;id:string;role:string;disabled?:boolean}){const[state,formAction]=useActionState(action,null);const{pending}=useFormStatus();return <form action={formAction} className="flex items-center gap-2"><input type="hidden" name="id" value={id}/><select name="role" defaultValue={role} disabled={disabled} className="h-9 rounded-md border border-mist-strong bg-paper-raised px-2 text-xs"><option value="owner">Owner</option><option value="admin">Admin</option><option value="manager">Manager</option><option value="receptionist">Receptionist</option></select><Button type="submit" size="sm" variant="secondary" disabled={disabled} isLoading={pending}>Save</Button>{state?.error&&<span className="text-xs text-clay">{state.error}</span>}</form>}
