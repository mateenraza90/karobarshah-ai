# Voice Agent (reserved)

No feature logic here yet — Milestone 0 is foundation only. Per
`docs/ARCHITECTURE.md` (Milestone 9), voice calls will eventually surface
in the same Calls inbox as WhatsApp conversations, not a separate route —
this folder holds the voice-specific orchestration (STT/TTS pipeline,
telephony webhook handling) that feeds into it.
