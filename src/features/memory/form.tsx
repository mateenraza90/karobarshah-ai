"use client";
import {useActionState} from "react"; import {useFormStatus} from "react-dom"; import {Button} from "@/components/ui/button"; import {Input} from "@/components/ui/input"; import type {ActionState} from "@/types";
export function MemoryForm({action,item}:{action:(p:ActionState,f:FormData)=>Promise<ActionState>;item?:{id:string;type:string;title:string|null;content:string}}){const[state,formAction]=useActionState(action,null);const{pending}=useFormStatus();return <form action={formAction} className="grid gap-4 rounded-md border border-mist p-4"><input type="hidden" name="id" value={item?.id??""}/><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Type<select name="type" defaultValue={item?.type??"faq"} className="mt-1 h-10 w-full rounded-md border border-mist-strong bg-paper-raised px-3"><option value="faq">FAQ</option><option value="policy">Policy</option><option value="service">Service knowledge</option><option value="hours">Hours</option></select></label><label className="text-sm font-medium">Title<Input name="title" defaultValue={item?.title??""} className="mt-1"/></label></div><label className="text-sm font-medium">Content<textarea name="content" defaultValue={item?.content??""} required rows={5} className="mt-1 w-full rounded-md border border-mist-strong bg-paper-raised p-3 text-sm"/></label><div className="flex items-center gap-3"><Button type="submit" isLoading={pending}>{item?"Save changes":"Add memory"}</Button>{state?.error&&<p role="alert" className="text-sm text-clay">{state.error}</p>}{state?.message&&<p role="status" className="text-sm text-ledger">{state.message}</p>}</div></form>}
